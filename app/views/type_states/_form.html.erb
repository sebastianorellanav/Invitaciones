<%= form_with(model: type_state) do |form| %>
  <% if type_state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_state.errors.count, "error") %> prohibited this type_state from being saved:</h2>

      <ul>
        <% type_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
